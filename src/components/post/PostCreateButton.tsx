import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const PostCreateButton = () => {
  return (
    <View>
      <Text>PostCreateButton</Text>
    </View>
  );
};

export default PostCreateButton;

const styles = StyleSheet.create({});
